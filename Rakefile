ENV["SINATRA_ENV"] ||= "development" #creates separate Sinatra environment for development purposes?

require_relative './config/environment' #requires all pertinent files when using rake
require 'sinatra/activerecord/rake' #requires rake functionality that is depended on by sinatra-activerecord, only needed for rake.
